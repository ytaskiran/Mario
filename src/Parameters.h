#pragma once

// General parameters for the game

constexpr int SCREEN_WIDTH = 800;				// Window screen width
constexpr int SCREEN_HEIGHT = 600;				// Window screen height
constexpr size_t _TILE_SIZE = 15;				// Tile size for the tile map
constexpr size_t _FLOOR_HEIGHT = 4;				// Height of the floor at the bottom
constexpr size_t _MARIO_MAX_LIVES = 3;			// Starting live count for Mario
constexpr float _TURTLE_MAX_SPEED_X = 9.0;		// Turtles' max x speed
constexpr float _TURTLE_MAX_SPEED_Y = 7.0;		// Turtles' max y speed
constexpr float _MARIO_MAX_SPEED = 10.0;		// Mario terminal velocity for y axis
constexpr float _MARIO_SPEED = 6.0;				// Default Mario speed
constexpr float _MARIO_JUMP_SPEED = -16.0;		// Default Mario jump speed
constexpr float _DEF_ANIMATION_OFFSET = 7.0;	// Value for the animation offset
